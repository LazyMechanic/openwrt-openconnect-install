Script to install package `openconnect`, `luci-proto-openconnect` and all dependencies

Single line install:
```
sh <(wget -O - https://raw.githubusercontent.com/LazyMechanic/openwrt-openconnect-install/refs/heads/master/install.sh)
```